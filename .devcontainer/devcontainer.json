{
	"name": "Flutter",
	"image": "mcr.microsoft.com/devcontainers/flutter:0",

	"customizations": {
		"vscode": {
			"extensions": [
				"Dart-Code.dart-code",
				"Dart-Code.flutter"
			]
		}
	},

	"forwardPorts": [9000],

	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y openjdk-11-jdk android-sdk nodejs npm python3 golang && flutter precache && flutter pub get",

	"remoteUser": "vscode"
}
